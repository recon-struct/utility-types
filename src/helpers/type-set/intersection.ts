/**
 * Alias of `A & B`. The [Intersection](https://en.wikipedia.org/wiki/Intersection_(set_theory))
 * `A ∩ B` of `A` and `B`
 * @typeParam A - The first set.
 * @typeParam B - The second set.
 * @group Type Set
 */
export type Intersection<A, B> = A & B
