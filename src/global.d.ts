/**
 * This is a mock our tests actually run in the `tsc` compiler
 */
declare function describe(description: string, spec: () => void): void

/**
 * This is a mock our tests actually run in the `tsc` compiler
 */
declare function it(description: string, spec: () => void): void
