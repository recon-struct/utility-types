/**
 * The multiplicative identity element for bigint
 * @alpha
 * @group Identity
 * @example
 * ```
 * type Ex = BigintMultiplicativeIdentity // 1n
 * ```
 */
export type BigintMultiplicativeIdentity = 1n
