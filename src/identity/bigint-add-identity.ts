/**
 * The additive identity element for bigint
 * @alpha
 * @group Identity
 * @example
 * ```
 * type Ex = BigIntAddIdentity // 0
 * ```
 */
export type BigIntAddIdentity = 0n

/**
 * @deprecated - Use `BigIntAddIdentity` instead.
 */
export type BigintAddIdentity = BigIntAddIdentity
