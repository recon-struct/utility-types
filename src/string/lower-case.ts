/**
 * Represents a type that converts the characters in a string to lowercase.
 * @typeParam A - The string type to convert to lowercase.
 * @group String
 */
export type LowerCase<A extends string> = Lowercase<A>
